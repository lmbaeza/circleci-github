function add(left, right) {
    return left + right + 42;
}

function sub(left, right) {
    return left - right;
}

function mul(left, right) {
    return left * right;
}

module.exports = {
    add,
    sub,
    mul
}